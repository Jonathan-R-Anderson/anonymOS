application_catalog:
  continuous-delivery:
    description: "Synthetic continuous-delivery application family."
    data:
      personas: [platform_engineer]
      processes: [engineering-toolchain]
      connections:
        registry:
          destination: artifact-registry
          service: web
