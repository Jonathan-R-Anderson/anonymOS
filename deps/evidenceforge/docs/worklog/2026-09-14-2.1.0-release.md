# 2.1.0 release preparation

The maintainer requested a new release after merging PR #419 into `dev` at
`133107ef7bf6437734bf6a46e33a1a6f5f595060`. Prepare the version bump on `dev` and
a release PR to `main`; merging and publication remain manual maintainer actions.

The previous release is 2.0.1. The non-breaking native Windows feature commit
`2c333149` requires a minor bump under the repository's conventional-commit policy,
so the next version is 2.1.0. The remote `v2.1.0` tag was absent during preparation.
The changelog groups every non-merge commit since `origin/main`; all three package
version declarations must match. Obsolete Windows-groundwork notes in
`CONTRIBUTING.md` and `docs/reference/GENERATION_CHECKPOINTS.md` are reconciled
with the implemented backend and its documented storage contract.

Local macOS 26.6.2 arm64 / Python 3.12.9 release coverage passed: **8,644 passed,
67 skipped, 2,023 deselected**, 788.30s, **75.35% coverage** against the required
70% minimum. The complete log is `/tmp/eforge-release-2.1.0-coverage.log`.
The full slow suite passed separately without coverage: **1,788 passed, 4 Windows-only
cases skipped, 8,942 deselected**, 1,129.53s. Its complete log is
`/tmp/eforge-release-2.1.0-slow.log`. Ruff lint/format and generation behavior
revision 89 validation pass; the behavior digest remains
`120dd81053c2116d467a2637f2ccc52e0f155ce4b546a114acde26eee4e9705c`.

`uv sync --all-extras --offline` regenerated only the package version in `uv.lock`;
no dependency versions changed. The imported version and installed package metadata
both report 2.1.0. Offline source-distribution and wheel builds pass; the wheel
includes the native Windows backend and conditional Windows timezone dependency.
Builds are retained under `/tmp/eforge-2.1.0-dist/` and have not been published.

The merged feature revision `59678514` passed every required check in
[run 34899284201](https://github.com/Cisco-Talos/EvidenceForge/actions/runs/34899284201).
The native Windows routine job completed in 24m 32s with the new 45-minute limit.
Linux routine, Windows durability, lint, and `Required CI` also passed.

The release PR must pass `Required CI`, `Required Release CI`, and the release version guard.
GitHub creates the annotated tag and Release automatically after a maintainer
merges to `main`. No tag, release, repository protection change, or automatic
merge is part of this preparation.
